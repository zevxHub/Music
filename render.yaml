services:
  - type: web
    name: music-recognition-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    aptpackages:
      - ffmpeg
    envVars:
      - key: DISCORD_TOKEN
        sync: false
